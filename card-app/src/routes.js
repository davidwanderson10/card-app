import Home from './index'

export const routeMain      = `/insights`
export const routeCreate   = `/insight/create`

export const reserva_de_salas_routes = [
    { exact: true, path: routeMain, component: ReservaDeSalas }
    { exact: true, path: routeMain, component: ReservaDeSalas }
]